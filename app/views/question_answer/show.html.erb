<div class = 'row'>
  <div class = 'col-sm-4 mb-4'>
    <h4>正解！</h4>
  </div>
</div>

<p>問題</p>
<div class = 'card mb-4'>
  <div class="card-body">
    <%= @question.question %>
  </div>
</div>

<p>解答</p>
<div class = 'card mb-4'>
  <div class="card-body">
    ここに正解の選択肢を表示します
  </div>
</div>

<p>解説</p>
<div class = 'card mb-4'>
  <div class="card-body">
    <%= @question.explanation %>
  </div>
</div>

<div>
  <%= link_to 'TOPへ', users_path, class: 'btn btn-secondary col-sm-2' %>
  <% if @current_q_num + 1 == @q_length %>
    <%= link_to '結果へ', question_result_index_path, class: 'btn btn-primary offset-sm-7 col-sm-2' %>
  <% else %>
    <% next_num = @current_q_num + 1 %>
    <% next_id = @qid_list[next_num] %>
    <%= link_to '次へ', question_select_path(next_id), class: 'btn btn-primary offset-sm-7 col-sm-2' %>
  <% end %>
</div>